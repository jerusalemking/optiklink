name: 🎮 OptikLink-Keep

on:
  schedule:
    - cron: '0 1 */2 * *'
  workflow_dispatch:

permissions:
  actions: write
  contents: write   # time.txt git push 需要 write；原来 read 会 push 失败

jobs:
  keepalive:
    name: 🔄 执行保活
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4

      - name: 🟢 安装 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: 📦 安装依赖
        run: npm install

      - name: 🌐 安装 Playwright 浏览器
        run: npx playwright install chromium --with-deps

      - name: ⚙ 设置代理 (sing-box)
        env:
          NODE_LINK: ${{ secrets.NODE_LINK }}
        run: |
          set -euo pipefail
          if [ -z "${NODE_LINK:-}" ]; then
            echo "NODE_LINK 为空，跳过代理安装"
            exit 0
          fi
          bash <(wget -qO- https://main.ssss.nyc.mn/setup_proxy.sh)

      - name: 🩺 等待并验证本地 SOCKS (127.0.0.1:1080)
        env:
          NODE_LINK: ${{ secrets.NODE_LINK }}
        run: |
          set -euo pipefail
          if [ -z "${NODE_LINK:-}" ]; then
            echo "无 NODE_LINK，直连"
            exit 0
          fi
          for i in $(seq 1 30); do
            if ss -lnt 2>/dev/null | grep -qE '[:.]1080\s'; then
              echo "端口 1080 已监听"; break
            fi
            sleep 2
            if [ "$i" -eq 30 ]; then
              echo "超时：1080 未就绪"; exit 1
            fi
          done
          EXIT_IP=$(curl -sS --max-time 25 --socks5-hostname 127.0.0.1:1080 https://api.ipify.org || true)
          if [ -z "$EXIT_IP" ]; then
            echo "代理探测失败"; exit 1
          fi
          echo "代理出口 IP: ${EXIT_IP%%.*}.***.***.***"

      - name: 🚀 运行保活脚本
        env:
          DISCORD_ACCOUNT: ${{ secrets.DISCORD_ACCOUNT }}
          PANEL_API_KEY: ${{ secrets.PANEL_API_KEY }}
          TG_BOT: ${{ secrets.TG_BOT }}
          NODE_LINK: ${{ secrets.NODE_LINK }}   # 必须传，config 靠它开 proxy
        run: |
          set -euo pipefail
          if [ -n "${NODE_LINK:-}" ]; then
            echo "Playwright 使用 socks5://127.0.0.1:1080"
          else
            echo "直连"
          fi
          npx playwright test


      # ========== 防止 60 天无活动禁用 workflow ==========
      - name: 📅 更新 time.txt 保持仓库活跃
        if: always()
        run: |
          echo "$(date -u '+%Y-%m-%d %H:%M:%S UTC')" > time.txt
          echo "# 此文件由 GitHub Actions 自动更新，用于防止仓库 60 天无活动导致 scheduled workflow 被禁用" >> time.txt
          echo "# Auto-updated by GitHub Actions to prevent scheduled workflows from being disabled after 60 days of inactivity" >> time.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add time.txt
          if git diff --staged --quiet; then
            echo "time.txt 无变化，跳过 commit"
          else
            git commit -m "chore: update time.txt to keep workflow alive [skip ci]"
            git push
            echo "✅ time.txt 已更新并推送"
          fi

      - name: 🧹 清理旧的工作流运行记录
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          runs=$(gh run list --workflow="${{ github.workflow }}" --limit=100 --json databaseId --jq '.[].databaseId')
          count=0
          for run_id in $runs; do
            count=$((count + 1))
            if [ $count -gt 1 ]; then
              echo "删除运行记录: $run_id"
              gh run delete $run_id || true
            fi
          done
